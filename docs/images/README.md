# Images

Screenshots referenced from the README and guides (CLI output, GUI dialogs, Live Dashboard) go here. None are checked in yet — add PNGs here and link them with relative paths, e.g. `![CLI screenshot](images/cli-info.png)`.
